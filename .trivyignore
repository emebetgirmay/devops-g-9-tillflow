# Trivy config acceptances — owner + expiry required.
# G1: public ALB :80 for smoke until API Gateway lands (G1.5/G2).

# AVDxxx patterns — Trivy may report rule IDs; keep subnet fix in code instead of ignore.
